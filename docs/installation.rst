============
Installation
============

At the command line::

    $ pip install hpcc_i_spark

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv hpcc_i_spark
    $ pip install hpcc_i_spark
